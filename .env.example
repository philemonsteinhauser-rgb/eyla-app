# Anthropic API Key (für /api/chat Serverless Function)
# Holen unter: https://console.anthropic.com/settings/keys
# Diese Datei .env.example NICHT löschen.
# Echte Werte in eine neue Datei `.env.local` schreiben (wird durch .gitignore ignoriert).
# Für Vercel: Settings → Environment Variables → ANTHROPIC_API_KEY anlegen.
ANTHROPIC_API_KEY=sk-ant-...
